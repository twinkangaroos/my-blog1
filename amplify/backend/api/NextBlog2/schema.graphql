type PostList @model @auth(rules: [{allow: public}]) {
  id: ID!
  content: String
  type: String
  sort: Int
  post_id: String
}

type Post @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  content: String
  author: String
  show_date: AWSDate
}
 