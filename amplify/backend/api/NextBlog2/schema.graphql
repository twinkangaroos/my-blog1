type PostList @model @auth(rules: [{allow: public}]) {
  id: ID!
  Post: Post @hasOne
  conttent: String
  type: String
  sort: Int
}

type Post @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  content: String
  author: String
  show_date: AWSDate
}
 